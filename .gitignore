# OS and editors
.DS_Store
.~lock.*#
.idea/
.vscode/
*.swp

# Workspace tooling, not part of the thesis
.claude/

# --- what deliberately stays out of a public repository -------------------
# Third-party publisher PDFs. Redistribution is the publisher's right, not
# ours: the local copies stay here, the thesis cites DOIs.
theoretical/library/

# Correspondence with the supervisors. Third parties who did not consent to
# publication, and none of it is needed to reproduce the work.
theoretical/supervision/

# Rendered audio (25 MB). The script and its script-input are tracked; git
# keeps binaries forever, and the render is reproducible from the two.
theoretical/audio/*
!theoretical/audio/gerar_audio.command
!theoretical/audio/roteiro_slr_pt-BR.txt
